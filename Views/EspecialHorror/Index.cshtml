@model IEnumerable<ProjetoCinema.Models.EspecialHorror>

@{
    ViewData["Title"] = "Index";
}

<h1>Mês do Horror</h1>

<p>
    <a asp-action="Create">Criar novo</a>
</p>
<table class="table">
    <div>
        @using (Html.BeginForm("Index", "Especial de horror", FormMethod.Get))
        {
                    <p>
                        Digite aqui sua pesquisa: @Html.TextBox("Pesquisa")
                        <input type="submit" value="Pesquisar" class="button" />
                    </p>
        }
 </div>
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Filmes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Classificacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HrSesssao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Filmes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Classificacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HrSesssao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
